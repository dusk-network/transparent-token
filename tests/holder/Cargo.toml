[package]
name = "tt-holder-contract"
version.workspace = true
edition.workspace = true

[dependencies]
tt-core = { workspace = true }

dusk-core = { workspace = true, features = ["abi-dlmalloc"] }

[lib]
crate-type = ["cdylib"]
